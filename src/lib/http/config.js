import store from '@/vuex'

export default {
  domain: 'larave-code.dy.com',
  rootUrl:'http://larave-gen.dy.com',
  baseURL: 'http://larave-gen.dy.com/api',
  timeout: 5000,
  headers: {
    'X-Requested-With': 'XMLHttpRequest'
  }
}
